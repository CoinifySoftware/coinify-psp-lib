import { IS_DEV } from './environment'

export class Coinify {
  public static registerCard( options: any ) {
  }
  public static handleTradePaymentInfo( tradeInfo: any ) {
  }
  public static applyCardToTradeTransferInDetails( tradeInfo: any ) {
  }
}
